/**
 * @outerdocs Phaser.GameObjects.Image
 * @memberof ExtendedPhaserObjects
 * @extends Phaser.GameObjects.Image
 * @extends Object
 * @description Some object that's been extended
 * @see {@link ExtendedPhaserObjects a link to something else}
 */
class myImage extends Object{
    /**
     * @param {Phaser.Scene} scene - the scene to make the object in
     * @param {number} x - The x in the scene
     * @param {number} y - The y in the scene
     * @param {Phaser.Textures.Texture|string} texture - The key of the texture
     * @param {string|number} frame - the frame of the texture
     */
    constructor(scene, x, y, texture, frame) {

    }

    /**
     * @outerdocs Phaser
     */
    someOtherFunction(phaserParm) {

    }
}